# # --- ZMK Studio および ポインティングデバイスの設定 ---
# # ZMK Studioのリモートロック機能を無効化（通常nのまま）
# CONFIG_ZMK_STUDIO_LOCKING=n 
# # ポインティングデバイス（トラックボールなど）サポートを有効化
# CONFIG_ZMK_POINTING=y 
# # スクロール動作をより滑らかにする機能を有効化
# CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y 
# # 使用するPixArt PAW3222ドライバを有効化
# CONFIG_PAW3222=y 


# # --- 分割キーボードの役割定義 ---
# # このハーフ（右側を想定）をホストと通信するセントラル（親）として定義
# CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y 


# # --- 入力処理の安定化とスレッド設定 ---
# # 入力処理スレッドに割り当てるスタックメモリを増強（トラックボールの複雑な処理安定化のため）
# CONFIG_INPUT_THREAD_STACK_SIZE=4096


# # --- Bluetooth LE 接続の最適化 ---
# # BLE HIDレポートキューのサイズを増強（データ欠落対策）
# CONFIG_ZMK_BLE_KEYBOARD_REPORT_QUEUE_SIZE=90
# # BLE接続パラメータの実験的機能（低遅延化）を有効化
# CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
# # BLE接続間隔を最短（7.5ms）に設定（レポート頻度最大化のため）
# CONFIG_BT_PERIPHERAL_PREF_MIN_INT=6
# # BLE接続間隔の最大値も最短に設定
# CONFIG_BT_PERIPHERAL_PREF_MAX_INT=6


# # --- キースキャン安定化（キーボード全体のデバウンス設定） ---
# # 押下時のデバウンス時間を延長（誤入力を防ぐため）
# CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=10
# # 離した時のデバウンス時間を延長
# CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=10


# --- ZMK Studio および ポインティングデバイスの設定 ---
# ZMK Studioのリモートロック機能を無効化（デフォルト: n）
CONFIG_ZMK_STUDIO_LOCKING=n 
# ポインティングデバイス（トラックボールなど）サポートを有効化（デフォルト: n）
CONFIG_ZMK_POINTING=y 
# スクロール動作をより滑らかにする機能を有効化（デフォルト: n）
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y 
# 使用するPixArt PAW3222ドライバを有効化（デフォルト: n）
CONFIG_PAW3222=y 

# --- 分割キーボードの役割定義 ---
# このハーフをホストと通信するセントラル（親）として定義（デフォルト: n）
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y 

# --- 電源管理とスリープ設定 (バッテリー対応) ---
# ZMKのスリープ機能を有効化（デフォルト: n）
CONFIG_ZMK_SLEEP=y 
# 5分 (300000ms) の非活動でアイドル状態に移行（デフォルト: 30000ms）
CONFIG_ZMK_IDLE_TIMEOUT=300000 
# 15分 (900000ms) の非活動でディープスリープに移行（デフォルト: 1800000ms）
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000 

# --- 入力処理の安定化とスレッド設定 ---
# 入力処理スレッドに割り当てるスタックメモリを増強（デフォルト: 1024 or 2048）
CONFIG_INPUT_THREAD_STACK_SIZE=4096

# --- Bluetooth LE 接続の最適化 (バッテリー優先) ---
# BLE HIDレポートキューのサイズを増強（デフォルト: 64）
CONFIG_ZMK_BLE_KEYBOARD_REPORT_QUEUE_SIZE=90
# BLE接続パラメータの実験的機能（低遅延化）を有効化（デフォルト: n）
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
# BLE接続間隔を標準的な30ms（24*1.25ms）に設定し、バッテリー消費を抑える（デフォルト: 24 / 30ms）
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=24
# BLE接続間隔の最大値も標準的な30msに設定
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=24

# --- キースキャン安定化（キーボード全体のデバウンス設定） ---
# 押下時のデバウンス時間を延長（デフォルト: 5ms）
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=10
# 離した時のデバウンス時間を延長（デフォルト: 5ms）
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=10